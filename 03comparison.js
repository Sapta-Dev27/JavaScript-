console.log( 2 > 1)//returns a boolean of true //
console.log( 2 <1)// returns a boolean value of false //
console.log( 2 == 1) // returns  a boolean value of false //
console.log( "2">1) //here in javascript ,  "2" is converted from string to a number and then its being compared and it returns a value of true //
console.log('2' == 1 ) // here also '2' is converted from a string to a number and then its being compared , and returns a value of false //
console.log( '2' < 1 ) //here also '2' is converted from a string to a number and then is being comnpared , returns a  value of false //
/*comparison operators are like > , < ,>= , <= */
console.log( null > 0) // null means empty so comparing operators like >  , < , >= , <= , converts null to a number // // returns false //
console.log( null==0) // returns false //
console.log(null>=0) // returns true //
console.log( null <= 0) // returns true //
 console.log( undefined> 0)//returns false //
console.log( undefined < 0)//returns false //
console.log( undefined ==0)//returns false //
console.log( "2" === 1)// when we use a === operator , it does not only  compares the value of the operator but it also compares the datatypes // // returns a false value //